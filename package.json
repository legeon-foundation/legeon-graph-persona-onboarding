{
  "name": "legeon-genesis-onboarding-compactdapp",
  "version": "0.1.0",
  "private": true,
  "description": "Legeon Phase 1: Privacy-Preserving Global Onboarding & Identity System",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean"
  },
  "keywords": [
    "legeon",
    "cardano",
    "midnight",
    "onboarding",
    "privacy"
  ],
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
