{
  "name": "@legeon/config",
  "version": "0.1.0",
  "description": "Configuration types and schemas for Legeon onboarding system",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
