{
  "name": "@legeon/midnight-adapter",
  "version": "0.1.0",
  "description": "Midnight protocol adapter for Legeon onboarding system (mock provider)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@legeon/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
